#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks if character is uppercase
 *
 * @c: character to check
 *
 * Return: Always Success (0)
 *
 */

int _isupper(int c)
{

if
(c >= 'A' && c <= 'Z')
{
return (1);
}

else
{
return (0);
}
return (0);
}
