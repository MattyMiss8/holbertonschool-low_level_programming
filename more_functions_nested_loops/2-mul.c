#include "main.h"
#include <stdio.h>

/**
 * mul - multiple two intergers
 *
 * @a: number to multiply
 *
 * @b: number to multiply
 *
 * Return: Always Success (0)
 *
 */

int mul(int a, int b)
{
return (a * b);
}
