#include <stdio.h>
#include "main.h"

/**
* add - adds two integers and returns the result
* @a: the first integer
* @b: the second integer
*
* Return: the value of add n
*/

int add(int a, int b)

{
int n = a + b;

return (n);
}
