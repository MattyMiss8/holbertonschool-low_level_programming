#include "main.h"
#include <stdio.h>

/**
 * _abs - check the code
 *
 * Description: computes the absolute value of an integer
 *
 * @r: outputs the absolute value
 *
 * Return: Always 0 (Success)
 *
*/

int _abs(int r)
{
if (r >= 0)
{
return (r);
}
else
{
return (-r);
}

}
